# Xcollector Web 前端配置
#
# 复制成 .env.local 使用（.gitignore 已忽略它）：
#     cp .env.example .env.local
#
# ⚠ Vite 只在**构建时**把 VITE_* 内联进产物，改了必须重启 dev server
#   或重新 build，热更新不会生效。

# ============================================================
# 1. 不再有「预置令牌」这一项
# ============================================================
# 多用户之后，身份只有一个来源：用户在登录页粘贴、或在注册页拿到的那串
# UserToken（`xc_...`）。它由后端签发、绑定到具体的 user_id，数据按它隔离。
#
# 旧版本有 VITE_API_TOKEN / VITE_BOT_API_TOKEN 两个预置令牌，用来跳过登录页。
# **已经删掉了**，而且不要再加回来：
#   - 多用户之后能填的只有服务令牌（API_TOKEN），也就是 bot 的身份，
#     等于把「读写所有人的数据」明文烧进 dist/assets/*.js；
#   - 任何能打开页面的人都能从 bundle 里把令牌读走。
# 开发时多粘一次令牌，比这个后果便宜得多。
#
# 需要脚本化访问时，用后端签发的某个用户令牌调 API（curl / httpx），
# 不要让它出现在前端构建产物里。

# ============================================================
# 2. 后端 / bot 的地址（可选）
# ============================================================
# vite.config.js 里的两条 dev 代理默认指向本机：
#     /api  → http://127.0.0.1:8000   （后端）
#     /bot  → http://127.0.0.1:8082   （bot，前缀会被摘掉）
#
# ⚠ `/bot` 这条代理**只在开发服务器上有意义**：系统状态页是运营者视图，
#   它要的是 bot 的管理令牌，而生产部署**不该把 /bot 反代到公网**（见第 3 节）。
#   留着它只是为了在本机调试那个页面。
#
# ⚠ 注意：这两个是 vite.config.js 里用 process.env 读的，**写在这个 .env 文件里
#   不会生效** —— Vite 不会把非 VITE_ 前缀的变量注入 process.env。
#    要用的话得设成真实的环境变量：
#        PowerShell:  $env:XCOLLECTOR_BACKEND='http://192.168.1.10:8000'
#        bash:        export XCOLLECTOR_BACKEND=http://192.168.1.10:8000
#
# XCOLLECTOR_BACKEND=http://127.0.0.1:8000
# XCOLLECTOR_BOT=http://127.0.0.1:8082

# ============================================================
# 2b. 前端请求前缀（可选，**构建期**内联）
# ============================================================
# 默认值就是同源的 `/api` 与 `/bot`（见 src/api/client.js 与 src/api/bot.js）。
# 只有「前后端不同源、又确实要直连」时才需要改 —— 那时还得自己处理 CORS 与预检。
# 推荐同源反代，见下面第 3 节。
#
# VITE_API_BASE=https://api.example.com/api
# VITE_BOT_BASE=https://bot.example.com/bot

# ============================================================
# 3. 生产部署：只需要**两条**规则
# ============================================================
# npm run build 产出的是**纯静态站点，不带任何代理**。
# 这个仓库**不产出镜像、也不带 nginx** —— 你必须自己用一个 web server 托管
# dist/，并在那里提供两条规则（两者必须同源）：
#
#     /      静态文件 + SPA fallback
#     /api/  反代到后端 127.0.0.1:8000   （前缀保留、**原样转发 Authorization**）
#
#     /bot/  **不要反代到公网。**
#
# 为什么 /bot/ 不反代：那是 bot 的管理接口，只认管理令牌（API_TOKEN），
# 拿到它就等于拿到「以 bot 身份读写所有人的数据 + 直接往 QQ 发消息」的能力。
# 把这条路径摆到公网上，等于给每个能打开页面的人一个暴力破解入口 —— 而它唯一
# 的用途是给运营者看「系统状态」页，运营者完全可以在**本机**直接访问：
#
#     curl -H "Authorization: Bearer $API_TOKEN" http://127.0.0.1:8082/api/status
#
# 运营者要在浏览器里看那一页时，也可以只在**本机**跑 `npm run dev`
# （dev server 保留了 /bot 代理），把管理令牌填进状态页的「运营者访问」框。
#
# 反代 /api/ 时还要做一件事：**原样转发**浏览器带的 Authorization 头，
# 否则登录页拿到的 UserToken 会被丢掉；也**不要**在反代里无条件注入令牌 ——
# 那会让所有请求都变成 bot 身份，登录页就形同虚设了。
#
# 完整配置片段（nginx 与 Caddy 各一份）见
# xcollector-deploy/README.md 的「托管 dist」一节。
